{# Posted with the surrounding entity form: the field itself is added to that form by EventManager on FORM_BEFORE_BUILD #}
<div class="card mb-3">
    <div class="card-header">
        <h6 class="card-title mb-0">{{ labels.title }}</h6>
    </div>
    <div class="card-body">
        <div class="mb-3">
            <label class="form-label" for="sitemap_priority">{{ labels.field }}</label>
            <input type="number"
                   id="sitemap_priority"
                   name="{{ fieldName }}"
                   class="form-control"
                   step="0.1"
                   min="0"
                   max="1"
                   value="{{ sitemapPriority }}">
        </div>
    </div>
</div>
